/**
 * @nestai/contracts
 *
 * Shared types and interfaces for NestAI. All external integrations
 * (LLM, calendar, messaging, email, etc.) are defined here as interfaces
 * so that implementations can be swapped between mock and live providers
 * based on PROVIDER_MODE.
 */
export const NESTAI_CONTRACTS_VERSION = '0.0.0';
