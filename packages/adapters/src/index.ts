/**
 * @nestai/adapters
 *
 * Concrete implementations of the integration interfaces declared in
 * @nestai/contracts. Each adapter ships a mock and (later) a live variant,
 * selected by PROVIDER_MODE. This is a buildable stub.
 */
export const NESTAI_ADAPTERS_VERSION = '0.0.0';
