{
  "name": "@nestai/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "test": "echo \"no tests yet (@nestai/db)\" && exit 0",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -b --noEmit false",
    "migrate": "echo \"db:migrate stub (@nestai/db) - implemented by DB agent\" && exit 0",
    "seed": "echo \"db:seed stub (@nestai/db) - implemented by DB agent\" && exit 0"
  },
  "dependencies": {
    "@nestai/contracts": "workspace:*"
  }
}
