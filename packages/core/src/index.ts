/**
 * @nestai/core
 *
 * Core domain logic and orchestration for NestAI. Depends only on
 * @nestai/contracts so it stays decoupled from concrete adapters.
 * This is a buildable stub.
 */
export const NESTAI_CORE_VERSION = '0.0.0';
