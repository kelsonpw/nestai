# Select the API client implementation: 'mock' (default) or 'real'.
VITE_API_MODE=mock
# Base URL used only by the real client (not required for mock).
VITE_API_BASE_URL=http://localhost:3000
# Vite base path for GitHub Pages project hosting, e.g. /nestai/
VITE_BASE=/
